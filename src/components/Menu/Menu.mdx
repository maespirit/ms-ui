import { Canvas, Meta, Source } from '@storybook/blocks';
import * as MenuStories from './Menu.stories';

<Meta of={MenuStories} />

# Menu

Menus provide a simple approach to constructing customized dropdown components that offer strong accessibility with support for keyboard navigation.

<Canvas of={MenuStories.Exemple} layout='centered' sourceState='none' />
